import withCard from "./withCard";
import UserCard from "./UserCard";
import ProductCard from "./ProductCard";

export const UserCardWithWrapper = withCard(UserCard);
export const ProductCardWithWrapper = withCard(ProductCard);
